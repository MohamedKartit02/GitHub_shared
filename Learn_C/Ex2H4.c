/* This programs gives the characters of these two number 72 and 104*/
#include <stdio.h>

int main()
{
	char x, y;
	x = 72;
	y = 104;

	printf("This is the chracter correspanding to 72 is %c.\n", x);
	printf("This is the chracter correspanding to 104 is %c.\n", y);
	
	return 0;
}
